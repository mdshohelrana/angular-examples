import { AnchorDisabledDirective } from './anchor-disabled.directive';

describe('AnchorDisabledDirective', () => {
  it('should create an instance', () => {
    const directive = new AnchorDisabledDirective();
    expect(directive).toBeTruthy();
  });
});
