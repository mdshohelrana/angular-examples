import { SfValidateDirective } from './sf-validate.directive';

describe('SfValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new SfValidateDirective();
    expect(directive).toBeTruthy();
  });
});
