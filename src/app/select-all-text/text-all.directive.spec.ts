import { TextAllDirective } from './text-all.directive';

describe('TextAllDirective', () => {
  it('should create an instance', () => {
    const directive = new TextAllDirective();
    expect(directive).toBeTruthy();
  });
});
